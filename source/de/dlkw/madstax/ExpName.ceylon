shared class ExpName(namespaceName, localName)
{
    shared String? namespaceName;
    shared String localName;
}

shared class NSInfo(prefix, uriReference)
{
    shared String prefix;
    shared String uriReference;
}

